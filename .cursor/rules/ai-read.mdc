---
description:
globs:
alwaysApply: true
---

## 项目概述

这是一个基于 Cloudflare Workers 的邮箱管理系统，提供完整的邮件接收、存储、转发和管理功能。

## 参考仓库

- https://github.com/vwh/temp-mail - UI设计参考，简洁实用
- https://github.com/XRSec/feishu-bot-webhook-action - 飞书机器人webhook实现
- https://github.com/webbertakken/email-worker - 邮件处理和webhook发送参考
- https://github.com/dreamhunter2333/cloudflare_temp_email - Cloudflare临时邮箱实现参考 **重点参考对象**

## 核心功能

### 邮件处理
1. **邮件接收**：通过 Cloudflare 电子邮件路由接收电子邮件
2. **数据存储**：将邮件数据存储在 Cloudflare D1 数据库中 可能考虑将邮件原始数据存储到 R2
3. **附件支持**：在 Cloudflare R2 中存储最大 50MB 的邮件附件
4. **API端点**：为邮件和附件提供完整的 REST API: api-documentation.yml
5. **自动清理**：可自定义时间的邮件和附件自动清理
6. **多文件类型**：支持文档、图像、压缩包等多种文件类型

### 系统架构
- **前后端分离**：前端通过 ASSETS 绑定服务，后端提供 REST API
- **配置管理**：使用 Cloudflare 机密存储敏感配置
- **多域名支持**：支持多个邮箱域名，自动匹配
- **数据库**：使用 Cloudflare D1 SQLite 数据库
- **存储**：使用 Cloudflare R2 存储附件
- **API设计**：统一的 REST API 路径，符合 OpenAPI 3.0 规范

### 前端
- 使用 pinia element-plus typescript vite Axios dayjs vueuse
- Using dynamic import() to code-split the application

### 📋 开发规则

1. **数据库操作** - 无需复杂迁移，直接修改 `db/schema.sql` 后执行 `npm run db:init`
2. **文档更新** - 每次功能开发完成后必须更新完善 .cursor/rules/ai-read.mdc
3. **API维护** - 开发前必须先仔细阅读 api/api-doc.yml 如果有修改请进行更新
4. **更新日志** - 开发以及更新日志统一保存到 docs/UPDATE_LOG.md 文件
5. **产品定义** - 该项目不叫临时邮箱管理系统，叫 邮箱管理系统 简写 cem 全拼 cloudflare email manager
6. **缓存机制** - 该项目统一使用localStorage进行缓存，所有缓存数据存储在localStorage中，使用`cem_cache_`前缀，最大缓存限制50MB。不再使用sessionStorage，完全基于localStorage实现。systemConfig也存储在localStorage中，确保系统配置持久化。当缓存策略相关代码发生修改时，同时需要更新 docs/CACHE_API.md
7. **流式相应** - 我正在计划前一流式相应 和 css 统一，我计划在 后面页面切换不在需要刷新整个页面，而是动态加载，可以参考 vue/src/App.vue 和 vue/src/views/LoginView.vue
8. **数据库结构同步** - worker/src/routes/database.ts 和 db/schema.sql 的 数据结构需要保持一致性
   - 已实现完整的11个表结构同步（包括security_audit表）
   - 使用SHA-256哈希存储用户密码
   - 数据库初始化时自动检查并创建管理员用户
   - 复用 '../utils/crypto' 中的 hashPassword 函数
   - 系统配置与schema.sql完全一致
   - SQL语句集中管理：DATABASE_SCHEMAS, DATABASE_INDEXES, INITIAL_DATA_SQL

### 🚀 开发环境

#### 环境配置
- **前端**：在宿主机运行 `cd vue && npm run dev`
- **后端**：在宿主机运行 `cd worker && npx wrangler dev`
- **数据库**：直接使用 `wrangler d1` 命令执行操作

#### 目录结构

```bash
.
├── cloudflare_temp_email 参考完整开源项目
│   ├── vue 前端
│   ├── mail-parser-wasm 邮件解析
│   └── worker 后端
├── db 
│   ├── db_init.js 数据库初始化脚本
│   └── schema.sql 数据库初始化结构
├── vue 前端项目
│   └── package.json
├── worker 后端项目
│   ├── package.json
│   ├── src 后端文件
│   └── wrangler.toml
├── scripts 脚本目录
│   ├── deploy.js 完整部署脚本
│   ├── dev.js 开发环境启动
│   └── db.js 数据库操作脚本
```

### 前端目录结构介绍
AppLoadingSpinner 负责 加载动画
AuthManager 负责验证用户权限
LoginView 负责显示登录页面 和登录登操作
MainLayoutView负责 登录后的页面展示

#### 开发命令

```bash
# 启动前端
cd vue && npm run dev

# 启动 Worker
cd worker && npx wrangler dev

# 数据库操作
npm run db:init        # 初始化数据库
npm run db:init:remote # 初始化远程数据库
npm run db:migrate     # 执行数据库迁移
npm run db:import      # 导入邮件数据

# 部署相关
npm run deploy         # 完整部署
npm run init           # 初始化部署
npm run clean          # 清理所有资源
```

#### 数据库操作

数据库操作直接通过 npm 脚本执行：
- 使用 `npm run db -- "SELECT * FROM users"` 执行 SQL 命令
- 使用 `npm run db -- U0VMRUNUICogRlJPTSB1c2Vycwo=` 执行 base64 编码的 SQL
- 使用 `npm run db:init` 初始化数据库
- 使用 `npm run db:init:remote` 初始化远程数据库
- 所有数据库操作都通过修改 `db/schema.sql` 完成，无需额外迁移脚本

#### 故障排除
- 如果遇到 `user_webhooks` 表不存在错误，执行 `npm run db:init` 重新初始化数据库
- 所有数据库操作都通过修改 `db/schema.sql` 完成，无需额外迁移脚本
- 测试用户名：admin/123456 test/123456