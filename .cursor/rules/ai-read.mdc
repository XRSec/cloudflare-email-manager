---
description:
globs:
alwaysApply: true
---

## 项目定义
Cloudflare Email Manager (CEM) - 基于 Cloudflare Workers 的邮箱管理系统

## 核心架构
- **前端**: pinia + element-plus + vite + typescript，动态加载，单页应用
- **后端**: Cloudflare Workers + D1(SQLite) + R2(附件存储)
- **模式**: 单管理员模式（无用户注册、邮箱申请等功能）
- **API**: REST API，参考 `api/api-doc.yml`

## 参考项目
- https://github.com/dreamhunter2333/cloudflare_temp_email - 重点参考
- https://github.com/vwh/temp-mail - UI参考
- https://github.com/webbertakken/email-worker - 邮件处理参考

## 开发规则（强制）

### 0. ⚠️ 禁止生成文档
- **禁止**主动创建任何 markdown 文档（*.md、README等）
- **禁止**生成冗长说明，直接编码实现
- 仅在用户明确要求时才创建文档

### 1. 数据库操作
- 直接修改 `db/schema.sql`，执行 `npm run db:init` 生效
- `worker/src/routes/database.ts` 必须与 `db/schema.sql` 保持同步
- SQL集中管理：DATABASE_SCHEMAS, DATABASE_INDEXES, INITIAL_DATA_SQL

### 2. API维护
- 开发前阅读 `api/api-doc.yml`，修改后同步更新

### 3. 缓存策略
- 统一使用 localStorage，前缀 `cem_cache_`，限制 50MB
- systemConfig 等存储在 localStorage

### 4. 前端结构
- AppLoadingSpinner: 加载动画
- AuthManager: 权限验证
- LoginView: 登录页面
- MainLayoutView: 主布局
- 动态加载路由，参考 `vue/src/App.vue`

### 5. 密码存储
- SHA-256 哈希，复用 `worker/src/utils/crypto.ts` 的 hashPassword 函数

## 目录结构
```
db/schema.sql          数据库结构
vue/                   前端（vite）
worker/src/            后端（Cloudflare Workers）
scripts/               脚本（deploy.js, db.js）
api/api-doc.yml        API文档
```

## 常用命令
```bash
cd vue && npm run dev           # 前端开发
cd worker && npx wrangler dev   # 后端开发
npm run db:init                 # 初始化本地数据库
npm run db:init:remote          # 初始化远程数据库
npm run deploy                  # 完整部署
```

## 测试账号
admin/123456