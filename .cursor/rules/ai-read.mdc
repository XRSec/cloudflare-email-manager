---
description:
globs:
alwaysApply: true
---

## 项目概述

这是一个基于 Cloudflare Workers 的邮箱管理系统，提供完整的邮件接收、存储、转发和管理功能。

## 参考仓库

- https://github.com/vwh/temp-mail - UI设计参考，简洁实用
- https://github.com/XRSec/feishu-bot-webhook-action - 飞书机器人webhook实现
- https://github.com/webbertakken/email-worker - 邮件处理和webhook发送参考
- https://github.com/dreamhunter2333/cloudflare_temp_email - Cloudflare临时邮箱实现参考 **重点参考对象**

## 核心功能

### 邮件处理
1. **邮件接收**：通过 Cloudflare 电子邮件路由接收电子邮件
2. **数据存储**：将邮件数据存储在 Cloudflare D1 数据库中 可能考虑将邮件原始数据存储到 R2
3. **附件支持**：在 Cloudflare R2 中存储最大 50MB 的邮件附件
4. **API端点**：为邮件和附件提供完整的 REST API: api-documentation.yml
5. **自动清理**：可自定义时间的邮件和附件自动清理
6. **多文件类型**：支持文档、图像、压缩包等多种文件类型

### 系统架构
- **前后端合并**：静态文件都存储在 Workers 中
- **配置管理**：使用 Cloudflare 机密存储敏感配置
- **多域名支持**：支持多个邮箱域名，自动匹配
- **数据库**：使用 Cloudflare D1 SQLite 数据库
- **存储**：使用 Cloudflare R2 存储附件

### 📋 开发规则

1. **数据库操作** - 无需复杂迁移，直接修改 `db/schema.sql` 后执行 `npm run db:init`
2. **文档更新** - 每次功能开发完成后必须更新完善 .cursor/rules/ai-read.mdc
3. **API维护** - 开发前必须先仔细阅读 api/api-doc.yml 如果有修改请进行更新
4. **更新日志** - 开发以及更新日志统一保存到 docs/UPDATE_LOG.md 文件

### 🚀 部署说明

#### 目录结构

```bash
.
├── cloudflare_temp_email 参考完整开源项目
│   ├── frontend 前端
│   ├── mail-parser-wasm 邮件解析
│   └── worker 后端
├── db 
│   ├── db_init.js 数据库 初始化脚本 带删除功能
│   └── schema.sql 数据库初始化结构，不带删除功能
├── deploy.js 部署项目脚本 主要是任务创建 R2 D1 KV 然后部署
├── frontend 前端项目 正在开发中
│   └── package.json
└── worker 后端项目 正在开发中
    ├── Dockerfile 调式用的 目前使用现成的容器 node
    ├── email_90692f1d-0590-4d03-ac3c-0d9d1120dc3e.eml cloudflare 接收到的原始邮件 eml 
    ├── email_analysis_report.md 邮件分析报告
    ├── package.json
    ├── src 后端文件
    └── wrangler.toml
```

#### 命令解析

**注意，现在在升级的过程中，有很多错误，可能下面的指令无法使用，因为 wrangler.toml 和 package.json 迁移到 worker 内了 
我们的 操作系统不满足 wrangler 的要求，所以和 wrangler 有关的内容我们需要使用 node 容器去操作
操作方法：
1. docker exec -it node bash -c 'wrangler dev'
2. docker exec -it node bash -c 'node xxx.js'
3. wrangler d1 execute cem-db --command "sql" --json

#### **故障排除**：
   - 如果遇到 `user_webhooks` 表不存在错误，执行 `npm run db:init` 重新初始化数据库
   - 所有数据库操作都通过修改 `db/schema.sql` 完成，无需额外迁移脚本
