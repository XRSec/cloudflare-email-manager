我认为这些仓库对我的代码很有帮助：

- 当然你也可以索引一些其他的仓库
- https://github.com/vwh/temp-mail
- https://github.com/XRSec/feishu-bot-webhook-action
- https://github.com/webbertakken/email-worker
- https://github.com/dreamhunter2333/cloudflare_temp_email

我想做的效果是：

1. 通过 Cloudflare 电子邮件路由接收电子邮件。
2. 将电子邮件数据存储在 Cloudflare D1 数据库中。
3. **附件支持**：在 Cloudflare R2 中存储最大 50MB 的电子邮件附件。
4. 为电子邮件和附件提供全面的 API 端点。
5. 自动清理旧的电子邮件和附件。可自定义时间
6. 支持多种文件类型，包括文档、图像和档案。
7. 配置信息使用 cloudflare 的 机密
8. 前后端合并，静态文件都存在 workers

管理员：

1. 有一个静态网页可以配置转发列表
    1. 比如 来自 xxx@xxx.xxx 的邮件包含关键字的邮件 转发到 xxx 的 webhook
    2. webhook 支持验签
2. 有一个页面负责创建，管理用户
3. 管理员有是否开放自由注册的权限
4. 管理员有一个页面可以配置 什么邮箱来的邮件转发到什么地址，同时可以过滤邮件发送者，邮件类型，邮件内容是否包含指定文本 ，可自由想象，UI 页面怎么写，最好再配置下验证邮件是否在 cloudflare 已验证，有一个按钮允许管理员向用户发送 用户信息，用户的 EMAIL PREFIX  EMAIL PASSWORD

普通用户：

1. 用户首次使用 需要填写 EMAIL PREFIX 和 EMAIL PASSWORD 才能看邮件，不支持越权
2. 用户支持修改 webhook，签名，密码，后端根据内容（不为空）进行更新(一个页面完成)，杜绝 sql 注入，修改完成提示内容，刷新，密码不返回前端，密码默认为空

综合：

1. 有一个页面支持查看 ， 过滤，删除邮件，邮件附件，支持过滤，时间，文件大小，从数据库读取，管理员允许查看所有邮件，用户只能查看自己的邮件
2. EMAIL PREFIX 使用随机数据，不可自定义，但是固定
3. webhook 支持 钉钉 和 飞书，支持后期新增其他平台，webhook 发送相关可以参考：https://github.com/webbertakken/email-worker
4. 我认为当前仓库的 UI 过于复杂，同样的 代码逻辑也很复杂，只适用于借鉴，不使用于我的项目，我认为 https://github.com/vwh/temp-mail 的 UI 够精简
5. 我们的邮箱域名不止一个,需要有自动切换的功能,如果能自动匹配那就更好了,比如收到邮件,匹配邮箱域

你需要做的是：

1. 调试模式不对普通用户显示，接口不对普通用户开放
2. 测试用户申请了 test@7k7k.com 但是 7k7k.com 不在我们的 系统设置 支持的域名 列表
3. 系统设置页面没有自动审核开关，核心策略就是自动通过用户申请
4. 系统设置页面 支持的域名 可以做成块状，右边有一个小 X 代表着删除，回车或者 加号即可添加新的域名，后端需要自动将空行筛除，支持的域名需要注册 是常用的域名后缀，避免输错
5. JWT 默认后端不给前端，除非有修改，JWT 默认为空，但是显示 10 个* 当数据为空的时候后端需要注意不更新
6. 无论是用户还是管理员 我的邮件页面后端只输出属于用户（收件方）的邮件，管理员有权限在 全部邮件 页面下查看所有邮件
7. 我的邮件页面只战士邮箱创建日期之后的邮件，这个后端处理
8. 邮箱申请需要注意 不能被别人申领，如果有人使用则不允许申请，可以做一个 api ，需要是用户，不区分是管理员还是普通用户
9. 邮箱申请 自动通过
10. webhook URL 可设置多条，成块状，加号和删除，向下创建新的列表，参考 openwrt webhook 的 密钥需要成双成对，可以考虑一左一右，Webhook 密钥提交完成后 在访问账户设置页面为空，但是可以更新，默认显示 10 个* 当数据为空的时候后端需要注意不更新
11. 调试模式 页面的 调试信息 似乎不完整
12. 用户管理页面 发送信息 功能改成向用户邮箱发送邮件，点击发送信息打开一个模态窗口，不是新建窗口，发件人为 admin 邮箱为 admin 的邮箱，可下拉选择，收件方在我的邮件页面可以看到这条邮件
13. 转发规则 页面 报错： Uncaught TypeError: AdminManager.editRule is not a function
14. 转发规则 页面 添加收件人 可以过滤的情况更全了，收件人从我的邮箱里面获取，可以做一个下拉选项
15. 刷新配置的时候并没有吧一些页面刷新，比如 用户管理页面，创建完用户的时候并没有刷新显示新的用户，点击刷新配置也没显示出来新的用户，刷新我认为做成一个全局按钮的意义不止这一点
16. 转发规则普通用户也可配置，但是邮箱只能使用自己有的
17. 邮件页面的搜索功能不太管用
18. 用户管理页面支持搜索过滤，基本的搜索框就可以，申请审核 支持搜索，搜索支持申请理由，邮箱，用户，时间
19. 所有的搜索都是动态刷新的，当然也可以使用回车 ，点击按钮，点击外部 作为一次搜索
20. 右上角的用户信息以后只显示用户名,前后端对齐
20. 完善 AI Must Read.md
